<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/drawer_header_grad"
    android:paddingTop="@dimen/regular_padding"
    android:paddingLeft="@dimen/regular_padding"
    android:paddingBottom="@dimen/regular_padding"
    android:paddingRight="@dimen/regular_padding"
    android:gravity="bottom">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher_round"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/drawer_title"
        android:layout_marginTop="@dimen/slim_margin"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/drawer_subtitle"
        android:layout_marginTop="@dimen/thin_mangin"/>

</LinearLayout>
